[project]
name = "sample-pdf-transcriber"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pymupdf>=1.28.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
